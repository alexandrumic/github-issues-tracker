// import { ViewStyle } from 'react-native';

export interface Props {
  size?: number | string;
  sizeHorizontal?: number | string;
  sizeVertical?: number | string;
  verticalLine?: boolean;
  horizontalLine?: boolean;
  isHidden?: boolean;
}

import { StyleSheet } from 'react-native';

import { Colors } from '../../../../theme';

export default StyleSheet.create({
  label: {
    fontSize: 13,
    fontWeight: '600',
    color: Colors.black,
    marginBottom: 5,
  },
  errorText: {
    color: Colors.red,
    fontSize: 12,
    marginTop: 5,
  },
});
